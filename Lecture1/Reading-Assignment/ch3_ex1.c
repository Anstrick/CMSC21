#include<stdio.h>

int main(void)
{
    //Initializes the variables and their types
    int i, j;
    float x, y;

    //Sets the values of the variables
    i = 10;
    j = 20;
    x = 43.2892f;
    y= 5527.0f;

    /*Prints a statement associating the variable names
      with their values*/
    printf("i = %d, j = %d, x = %f, y = %f\n", i, j, x, y);
}